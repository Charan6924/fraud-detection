{
  "name": "fraud-detection",
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "console": "sst console",
    "test": "jest --ci"
  },
  "devDependencies": {
    "@jest/types": "^30.4.1",
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "sst": "^4.17.1",
    "ts-jest": "^29.4.12",
    "ts-node": "^10.9.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@upstash/redis": "^1.38.2"
  }
}
